{
  "mcpServers": {
    "MetaMCP": {
      "command": "npx",
      "args": [
        "-y",
        "@metamcp/mcp-server-metamcp@latest"
      ],
      "env": {
        "METAMCP_API_KEY": "${env:METAMCP_API_KEY}"
      },
      "disabled": false,
      "alwaysAllow": []
    }
  }
}
